import { checkConsumerRawValues } from './validate';

console.log('[scan] Scanning consumer-editable files for raw token-law values...');
checkConsumerRawValues('scan');
console.log('[scan] Scan passed.');
